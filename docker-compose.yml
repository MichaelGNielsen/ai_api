services:
  ai-app:
    build: .
    env_file:
      - .env
    environment:
      - LLM_HOST=host.docker.internal
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - .:/app